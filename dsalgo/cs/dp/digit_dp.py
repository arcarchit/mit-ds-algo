"""

Find all the numbers in the range (L,R) which has exactly k non zero digits.

f(L,R) = f(0,R) - f(0,L-1)

f(0,X)


X = [1,0 3 4]

dp[pos, allowed_zeros, tight]
{

if pos == last and

limit = 9
if tight : limit x[pos]
ans = 0
for each no in limit
    if no_allowed (based on allowed_zeros)
    ans += sub

return ans


}




"""